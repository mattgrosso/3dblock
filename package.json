{
  "name": "3dblock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "yarn build && aws s3 sync dist/ s3://aroundtableround-3dblock --delete --profile personal-deploy && aws cloudfront create-invalidation --distribution-id E26TGWNT5TCXV1 --paths '/*' --profile personal-deploy > invalidation_output.txt"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "1.6.0"
  }
}
